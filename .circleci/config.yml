version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:13.7.0
    steps:
      - checkout
      - run:
          name: Install yarn
          command: "curl -o- -L https://yarnpkg.com/install.sh | bash"
      - restore_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
      - run:
          name: Install dependencies
          command: yarn install --frozen-lockfile
      - save_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
          paths:
            - ~/.cache/yarn
      - run:
          name: Create results directory
          command: mkdir -p /tmp/results
      - run:
          name: Lint code
          command:
            yarn lint --format junit --output-file /tmp/results/eslint.xml
          when: always
      - run:
          name: Run tests
          command: yarn test --ci --reporters=default --reporters=jest-junit
          environment:
            JEST_JUNIT_OUTPUT_DIR: /tmp/results
            JEST_JUNIT_OUTPUT_NAME: jest.xml
          when: always
      - store_test_results:
          path: /tmp/results
      - store_artifacts:
          path: /tmp/results
